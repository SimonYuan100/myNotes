# git 命令

```bash
# git init
初始化
# git add .
# git commit -m '描述'
# git push
# git pull

# git checkout file

# git config --global http.proxy http://xxx.com:8080
# git config --global https.proxy https://xxx.com:8080
